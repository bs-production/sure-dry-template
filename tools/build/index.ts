/**
 * Overview:
 * ==========
 * When the src gets build it generates a parcel-manifest.json.
 * This can then be used to build the prod files, which only includes
 * the stylesheets and scripts.
 *
 * In developing a new theme the styles and scripts are hosted on github
 * and redirected with https://combinatronics.com/ to apply the appropriate
 * file headers. These links are then used in the borders to enable quick testing
 * of the theme
 */
